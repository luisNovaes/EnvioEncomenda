@model IEnumerable<EnvioEncomenda.Models.Veiculos>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.VeiPlaca)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.VeiCapacid)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.VeiPlaca)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.VeiCapacid)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.VeiculoId }) |
            @Html.ActionLink("Details", "Details", new { id=item.VeiculoId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.VeiculoId })
        </td>
    </tr>
}

</table>
